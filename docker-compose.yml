name: apache-bigdata-siem

networks:
  siem-network:
    driver: bridge
    name: siem-network

volumes:
  namenode-data:
  datanode-1-data:
  datanode-2-data:
  postgres-data:
  kafka-data:
  hive-warehouse:
  superset_postgres_data:
  superset_data:
  zeppelin_data:
  zeppelin_notebooks:

services:
  namenode:
    image: bde2020/hadoop-namenode:2.0.0-hadoop3.2.1-java8
    container_name: namenode
    restart: unless-stopped
    environment:
      CLUSTER_NAME: siem-hdfs
      CORE_CONF_fs_defaultFS: hdfs://namenode:8020
      CORE_CONF_hadoop_http_staticuser_user: root
      HDFS_CONF_dfs_namenode_datanode_registration_ip___hostname___check: "false"
      HDFS_CONF_dfs_replication: "2"
    ports:
      - "9870:9870"
      - "8020:8020"
    volumes:
      - namenode-data:/hadoop/dfs/name
      - ./config/hadoop/core-site.xml:/etc/hadoop/core-site.xml:ro
      - ./config/hadoop/hdfs-site.xml:/etc/hadoop/hdfs-site.xml:ro
    networks:
      - siem-network

  datanode-1:
    image: bde2020/hadoop-datanode:2.0.0-hadoop3.2.1-java8
    container_name: datanode-1
    restart: unless-stopped
    environment:
      CORE_CONF_fs_defaultFS: hdfs://namenode:8020
      SERVICE_PRECONDITION: namenode:9870 namenode:8020
      HDFS_CONF_dfs_datanode_use_datanode_hostname: "true"
    depends_on:
      - namenode
    ports:
      - "9864:9864"
    volumes:
      - datanode-1-data:/hadoop/dfs/data
      - ./config/hadoop/core-site.xml:/etc/hadoop/core-site.xml:ro
      - ./config/hadoop/hdfs-site.xml:/etc/hadoop/hdfs-site.xml:ro
    networks:
      - siem-network

  datanode-2:
    image: bde2020/hadoop-datanode:2.0.0-hadoop3.2.1-java8
    container_name: datanode-2
    restart: unless-stopped
    environment:
      CORE_CONF_fs_defaultFS: hdfs://namenode:8020
      SERVICE_PRECONDITION: namenode:9870 namenode:8020
      HDFS_CONF_dfs_datanode_use_datanode_hostname: "true"
    depends_on:
      - namenode
    ports:
      - "9865:9864"
    volumes:
      - datanode-2-data:/hadoop/dfs/data
      - ./config/hadoop/core-site.xml:/etc/hadoop/core-site.xml:ro
      - ./config/hadoop/hdfs-site.xml:/etc/hadoop/hdfs-site.xml:ro
    networks:
      - siem-network

  postgres:
    image: postgres:15-alpine
    container_name: hive-metastore-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: metastore
      POSTGRES_USER: hive
      POSTGRES_PASSWORD: hivepass
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hive -d metastore"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - siem-network

  hive-metastore:
    image: apache/hive:4.0.0
    container_name: hive-metastore
    restart: unless-stopped
    environment:
      SERVICE_NAME: metastore
      DB_DRIVER: postgres
      SERVICE_OPTS: >-
        -Djavax.jdo.option.ConnectionDriverName=org.postgresql.Driver
        -Djavax.jdo.option.ConnectionURL=jdbc:postgresql://postgres:5432/metastore
        -Djavax.jdo.option.ConnectionUserName=hive
        -Djavax.jdo.option.ConnectionPassword=hivepass
      HADOOP_HEAPSIZE: "1024"
    depends_on:
      postgres:
        condition: service_healthy
      namenode:
        condition: service_started
      datanode-1:
        condition: service_started
      datanode-2:
        condition: service_started
    ports:
      - "9083:9083"
    volumes:
      - hive-warehouse:/opt/hive/data/warehouse
      - ./config/hive/hive-site.xml:/opt/hive/conf/hive-site.xml:ro
      - ./config/hadoop/core-site.xml:/opt/hadoop/etc/hadoop/core-site.xml:ro
      - ./config/hadoop/hdfs-site.xml:/opt/hadoop/etc/hadoop/hdfs-site.xml:ro
      - ./config/hive/postgresql-42.7.3.jar:/opt/hive/lib/postgresql-42.7.3.jar:ro
    networks:
      - siem-network

  hive-server2:
    image: apache/hive:4.0.0
    container_name: hive-server2
    restart: unless-stopped
    environment:
      SERVICE_NAME: hiveserver2
      DB_DRIVER: postgres
      IS_RESUME: "true"
      SERVICE_OPTS: >-
        -Dhive.metastore.uris=thrift://hive-metastore:9083
        -Djavax.jdo.option.ConnectionDriverName=org.postgresql.Driver
        -Djavax.jdo.option.ConnectionURL=jdbc:postgresql://postgres:5432/metastore
        -Djavax.jdo.option.ConnectionUserName=hive
        -Djavax.jdo.option.ConnectionPassword=hivepass
      HIVE_SERVER2_THRIFT_PORT: "10000"
    depends_on:
      hive-metastore:
        condition: service_started
    ports:
      - "10001:10000"
      - "10002:10002"
    volumes:
      - hive-warehouse:/opt/hive/data/warehouse
      - ./config/hive/hive-site.xml:/opt/hive/conf/hive-site.xml:ro
      - ./config/hadoop/core-site.xml:/opt/hadoop/etc/hadoop/core-site.xml:ro
      - ./config/hadoop/hdfs-site.xml:/opt/hadoop/etc/hadoop/hdfs-site.xml:ro
      - ./config/hive/postgresql-42.7.3.jar:/opt/hive/lib/postgresql-42.7.3.jar:ro
    networks:
      - siem-network

  kafka-broker:
    image: bitnamilegacy/kafka:3.7.1-debian-12-r9
    container_name: kafka-broker
    restart: unless-stopped
    environment:
      KAFKA_KRAFT_CLUSTER_ID: MkU3OEVBNTcwNTJENDM2Qk
      KAFKA_CFG_NODE_ID: "1"
      KAFKA_CFG_PROCESS_ROLES: broker,controller
      KAFKA_CFG_LISTENERS: PLAINTEXT://:9092,CONTROLLER://:9093
      KAFKA_CFG_ADVERTISED_LISTENERS: PLAINTEXT://kafka-broker:9092
      KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      KAFKA_CFG_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_CFG_CONTROLLER_QUORUM_VOTERS: 1@kafka-broker:9093
      KAFKA_CFG_AUTO_CREATE_TOPICS_ENABLE: "true"
      KAFKA_CFG_OFFSETS_TOPIC_REPLICATION_FACTOR: "1"
      KAFKA_CFG_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: "1"
      KAFKA_CFG_TRANSACTION_STATE_LOG_MIN_ISR: "1"
      ALLOW_PLAINTEXT_LISTENER: "yes"
    depends_on:
      hive-server2:
        condition: service_started
    ports:
      - "9092:9092"
    volumes:
      - kafka-data:/bitnami/kafka
    healthcheck:
      test: ["CMD-SHELL", "kafka-topics.sh --bootstrap-server localhost:9092 --list >/dev/null 2>&1"]
      interval: 15s
      timeout: 10s
      retries: 10
    networks:
      - siem-network

  spark-master:
    image: bitnamilegacy/spark:3.5.6-debian-12-r1
    container_name: spark-master
    restart: unless-stopped
    environment:
      SPARK_MODE: master
      SPARK_MASTER_HOST: spark-master
      SPARK_MASTER_PORT_NUMBER: "7077"
      SPARK_MASTER_WEBUI_PORT_NUMBER: "8080"
      SPARK_NO_DAEMONIZE: "yes"
      SPARK_RPC_AUTHENTICATION_ENABLED: "no"
      SPARK_RPC_ENCRYPTION_ENABLED: "no"
      SPARK_LOCAL_STORAGE_ENCRYPTION_ENABLED: "no"
      SPARK_SSL_ENABLED: "no"
      SPARK_CONF_DIR: /opt/bitnami/spark/conf
    command: >
      /bin/bash -c "
      /opt/bitnami/scripts/spark/run.sh &
      sleep 10 &&
      /opt/bitnami/spark/sbin/start-thriftserver.sh --master spark://spark-master:7077 --hiveconf hive.server2.thrift.port=10000 &&
      wait
      "
    depends_on:
      kafka-broker:
        condition: service_healthy
    ports:
      - "8080:8080"
      - "7077:7077"
      - "10000:10000"
    volumes:
      - ./config/spark/spark-defaults.conf:/opt/bitnami/spark/conf/spark-defaults.conf:ro
      - ./config/hadoop/core-site.xml:/opt/bitnami/spark/conf/core-site.xml:ro
      - ./config/hadoop/hdfs-site.xml:/opt/bitnami/spark/conf/hdfs-site.xml:ro
      - ./config/hive/hive-site.xml:/opt/bitnami/spark/conf/hive-site.xml:ro
      - ./jobs:/opt/bitnami/spark/jobs
    networks:
      - siem-network

  spark-worker-1:
    image: bitnamilegacy/spark:3.5.6-debian-12-r1
    container_name: spark-worker-1
    restart: unless-stopped
    environment:
      SPARK_MODE: worker
      SPARK_MASTER_URL: spark://spark-master:7077
      SPARK_WORKER_MEMORY: 2G
      SPARK_WORKER_CORES: "2"
      SPARK_WORKER_WEBUI_PORT: "8081"
      SPARK_NO_DAEMONIZE: "yes"
      SPARK_CONF_DIR: /opt/bitnami/spark/conf
    depends_on:
      spark-master:
        condition: service_started
    ports:
      - "8081:8081"
    volumes:
      - ./config/spark/spark-defaults.conf:/opt/bitnami/spark/conf/spark-defaults.conf:ro
      - ./config/hadoop/core-site.xml:/opt/bitnami/spark/conf/core-site.xml:ro
      - ./config/hadoop/hdfs-site.xml:/opt/bitnami/spark/conf/hdfs-site.xml:ro
      - ./config/hive/hive-site.xml:/opt/bitnami/spark/conf/hive-site.xml:ro
    networks:
      - siem-network

  spark-worker-2:
    image: bitnamilegacy/spark:3.5.6-debian-12-r1
    container_name: spark-worker-2
    restart: unless-stopped
    environment:
      SPARK_MODE: worker
      SPARK_MASTER_URL: spark://spark-master:7077
      SPARK_WORKER_MEMORY: 2G
      SPARK_WORKER_CORES: "2"
      SPARK_WORKER_WEBUI_PORT: "8081"
      SPARK_NO_DAEMONIZE: "yes"
      SPARK_CONF_DIR: /opt/bitnami/spark/conf
    depends_on:
      spark-master:
        condition: service_started
    ports:
      - "8082:8081"
    volumes:
      - ./config/spark/spark-defaults.conf:/opt/bitnami/spark/conf/spark-defaults.conf:ro
      - ./config/hadoop/core-site.xml:/opt/bitnami/spark/conf/core-site.xml:ro
      - ./config/hadoop/hdfs-site.xml:/opt/bitnami/spark/conf/hdfs-site.xml:ro
      - ./config/hive/hive-site.xml:/opt/bitnami/spark/conf/hive-site.xml:ro
    networks:
      - siem-network

  flog-web:
    build:
      context: ./flog
      dockerfile: Dockerfile
    container_name: flog-web
    restart: unless-stopped
    environment:
      KAFKA_BOOTSTRAP_SERVERS: kafka-broker:9092
      KAFKA_TOPIC: web-logs
      LOG_PROFILE: web
      BATCH_SIZE: "2000"
      LOOP_DELAY: "1"
    depends_on:
      spark-worker-1:
        condition: service_started
      spark-worker-2:
        condition: service_started
    networks:
      - siem-network

  flog-syslog:
    build:
      context: ./flog
      dockerfile: Dockerfile
    container_name: flog-syslog
    restart: unless-stopped
    environment:
      KAFKA_BOOTSTRAP_SERVERS: kafka-broker:9092
      KAFKA_TOPIC: syslogs
      LOG_PROFILE: syslog
      BATCH_SIZE: "2000"
      LOOP_DELAY: "1"
    depends_on:
      spark-worker-1:
        condition: service_started
      spark-worker-2:
        condition: service_started
    networks:
      - siem-network

  flog-app:
    build:
      context: ./flog
      dockerfile: Dockerfile
    container_name: flog-app
    restart: unless-stopped
    environment:
      KAFKA_BOOTSTRAP_SERVERS: kafka-broker:9092
      KAFKA_TOPIC: app-logs
      LOG_PROFILE: json
      BATCH_SIZE: "2000"
      LOOP_DELAY: "1"
    depends_on:
      spark-worker-1:
        condition: service_started
      spark-worker-2:
        condition: service_started
    networks:
      - siem-network

  # ===== SUPERSET =====
  superset-postgres:
    image: postgres:13-alpine
    container_name: superset-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: superset
      POSTGRES_PASSWORD: superset123
      POSTGRES_DB: superset
    volumes:
      - superset_postgres_data:/var/lib/postgresql/data
    networks:
      - siem-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U superset -d superset"]
      interval: 10s
      timeout: 5s
      retries: 5

  superset-redis:
    image: redis:7-alpine
    container_name: superset-redis
    restart: unless-stopped
    networks:
      - siem-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  superset:
    image: apache/superset:latest
    container_name: superset
    user: root
    restart: unless-stopped
    depends_on:
      superset-postgres:
        condition: service_healthy
      superset-redis:
        condition: service_healthy
    environment:
      SUPERSET_SECRET_KEY: f8d93a10-2b4a-4e2b-b893-ecf9e2b1029c
      SQLALCHEMY_DATABASE_URI: postgresql+psycopg2://superset:superset123@superset-postgres:5432/superset
      REDIS_CELERY_DB: "0"
      REDIS_BROKER_URL: redis://superset-redis:6379/0
      REDIS_RESULTS_DB: "1"
      REDIS_CACHE_DB: "2"
    ports:
      - "8088:8088"
    networks:
      - siem-network
    volumes:
      - superset_data:/app/superset_home
      - ./scripts:/app/scripts:ro
    command: >
      bash /app/scripts/superset_init.sh

  # ===== ZEPPELIN =====
  zeppelin:
    image: apache/zeppelin:0.11.1
    container_name: zeppelin
    restart: unless-stopped
    depends_on:
      spark-master:
        condition: service_started
    environment:
      - SPARK_MASTER=spark://spark-master:7077
      - MASTER=spark://spark-master:7077
      - ZEPPELIN_PORT=8080
      - ZEPPELIN_JAVA_OPTS=-Dspark.driver.memory=1g -Dspark.executor.memory=1g
    ports:
      - "8090:8080"
    networks:
      - siem-network
    volumes:
      - zeppelin_data:/zeppelin/logs
      - zeppelin_notebooks:/zeppelin/notebook
      - ./scripts:/zeppelin/scripts:ro
      - ./config/spark/spark-defaults.conf:/opt/spark/conf/spark-defaults.conf:ro
      - ./config/hadoop/core-site.xml:/opt/hadoop/etc/hadoop/core-site.xml:ro
      - ./config/hadoop/hdfs-site.xml:/opt/hadoop/etc/hadoop/hdfs-site.xml:ro
      - ./config/hive/hive-site.xml:/opt/hive/conf/hive-site.xml:ro
    command: ["bash", "/zeppelin/scripts/zeppelin_init.sh"]
